# Вклад в проект BookVault

Спасибо за интерес к проекту BookVault! Мы приветствуем любой вклад в развитие приложения.

## 🚀 Как начать

### 1. Форкните репозиторий
Создайте форк репозитория на GitHub и клонируйте его локально:

```bash
git clone https://github.com/ваш-username/bookvault.git
cd bookvault
```

### 2. Создайте виртуальное окружение

```bash
python -m venv .venv
source .venv/bin/activate  # macOS/Linux
# или
.venv\Scripts\activate  # Windows
```

### 3. Установите зависимости

```bash
pip install -r requirements.txt
```

### 4. Создайте ветку для вашей фичи

```bash
git checkout -b feature/your-feature-name
```

## 📝 Правила для Pull Request

### Перед отправкой PR убедитесь, что:

- [ ] Код компилируется без ошибок
- [ ] Все импорты работают корректно
- [ ] Соблюдён стиль кодирования проекта
- [ ] Добавлены docstrings для новых функций/классов
- [ ] Обновлён README.md (если необходимо)
- [ ] Коммиты имеют осмысленные сообщения

### Структура коммитов

Используйте понятные сообщения коммитов:

```
feat: добавить экспорт библиотеки в JSON
fix: исправить проблему с кодировкой UTF-8
docs: обновить инструкцию по установке
style: улучшить внешний вид кнопок
refactor: оптимизировать поиск по содержимому
```

## 🎨 Стиль кодирования

### Python
- Следуйте PEP 8
- Используйте type hints где это возможно
- Максимальная длина строки: 100 символов
- Используйте `from __future__ import annotations`

### Пример:

```python
from __future__ import annotations

from typing import Optional

def search_books(keyword: str, limit: int = 10) -> list[Book]:
    """Ищет книги по ключевому слову.
    
    Args:
        keyword: Ключевое слово для поиска.
        limit: Максимальное количество результатов.
        
    Returns:
        Список найденных книг.
    """
    # Ваш код здесь
    pass
```

### Docstrings
Используйте Google Style docstrings:

```python
def function_name(param1: str, param2: int) -> bool:
    """Краткое описание функции.
    
    Более подробное описание, если необходимо.
    
    Args:
        param1: Описание первого параметра.
        param2: Описание второго параметра.
        
    Returns:
        Описание возвращаемого значения.
        
    Raises:
        ValueError: Когда возникает эта ошибка.
    """
    pass
```

## 🏗️ Архитектура

### Слои приложения

1. **Data Layer** (`app/db.py`, `app/models.py`)
   - Работа с базой данных
   - Модели данных

2. **Service Layer** (`app/services/`)
   - Бизнес-логика
   - Работа с PDF
   - Сканирование файлов

3. **Presentation Layer** (`app/ui/`)
   - GUI компоненты
   - Модели представления
   - Делегаты отрисовки

### Добавление новой функции

1. Определите, в какой слой входит ваша функция
2. Создайте/обновите соответствующий сервис
3. Добавьте UI элементы (если необходимо)
4. Обновите README.md

## 🐛 Сообщение об ошибках

При создании Issue укажите:

- **Описание проблемы**: что происходит и что ожидается
- **Шаги воспроизведения**: как повторить ошибку
- **Окружение**: ОС, версия Python, версии зависимостей
- **Логи/скриншоты**: если применимо

### Шаблон Issue:

```markdown
**Описание**
Краткое описание проблемы.

**Воспроизведение**
1. Откройте приложение
2. Нажмите на '...'
3. Увидите ошибку

**Ожидаемое поведение**
Описание того, что должно происходить.

**Окружение**
- ОС: macOS 14.0
- Python: 3.11.5
- PySide6: 6.10.0
- PyMuPDF: 1.26.0
```

## 💡 Идеи для вклада

### Начинающим разработчикам
- Улучшить сообщения об ошибках
- Добавить больше подсказок (tooltips)
- Улучшить документацию
- Исправить опечатки

### Среднему уровню
- Добавить поддержку тегов
- Реализовать экспорт/импорт библиотеки
- Улучшить производительность поиска
- Добавить горячие клавиши

### Продвинутым
- Реализовать полнотекстовую индексацию
- Добавить поддержку EPUB/MOBI
- Интеграция с облачными хранилищами
- OCR для сканированных PDF

## 🧪 Тестирование

В настоящее время проект не имеет автоматических тестов. Перед отправкой PR:

1. Запустите приложение: `python main.py`
2. Протестируйте вашу функцию вручную
3. Проверьте, что существующий функционал работает
4. Протестируйте обе темы (светлую и тёмную)

## 📞 Связь

- **GitHub Issues**: для багов и предложений
- **Pull Requests**: для вклада в код
- **Discussions**: для общих вопросов

## 📄 Лицензия

Внося свой вклад, вы соглашаетесь, что ваш код будет лицензирован под MIT License.

---

**Спасибо за ваш вклад в BookVault!** 🙏📚